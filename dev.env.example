# Copy this file to dev.env and fill in your tokens.
# dev.env is gitignored and will not be committed.

# Hugging Face access token (required for downloading models/datasets).
HF_TOKEN=hf_your_token_here

# OpenAI API key (only required for `python inference.py --mode llm`).
OPENAI_API_KEY=sk-your_key_here
