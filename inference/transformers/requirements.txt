# Shared environment for inference/transformers (Python 3.12) -- one venv for all three
# leaves (llm, embedding, reranker), the correctness-baseline stack.
#
#   python3 -m venv .env_transformers && source .env_transformers/bin/activate
#
# AMD / ROCm 7.2 -- install torch and torchvision FIRST, from the ROCm index:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/rocm7.2
#   pip install -r requirements.txt
#
# NVIDIA / CUDA 13 -- same order, but from plain PyPI:
#   pip install torch==2.11.0          # the PyPI wheel is the CUDA 13 build (no --index-url)
#   pip install -r requirements.txt    # the torch/torchvision pins below are already satisfied
#   python -c "import torch;print(torch.__version__, torch.version.cuda)"   # 2.11.0 13.0
#   Add --index-url https://download.pytorch.org/whl/cu130 if you want the +cu130 tag.
#
# Do not pip-install flash-attn. It is a CUDA-only build with no prebuilt cu130 wheel, and
# the scripts select attn_implementation=sdpa. On CUDA pass --attn_impl sdpa explicitly,
# because the scripts auto-select `eager` when torch.version.hip is None.
sentence-transformers==5.7.0
torch==2.11.0                # Installs as 2.11.0+rocm7.2 on AMD; the plain PyPI wheel is the CUDA 13 build.
torchvision==0.26.0          # AutoProcessor image code for the multimodal Qwen3.8 checkpoint (llm leaf). Not needed for embedding/reranker.
transformers==5.5.0
accelerate==1.14.0
kernels>=0.12.0,<0.13        # llm leaf FP8 path. Respect the upper bound: 0.16.0 crashes the transformers 5.5.0 import.
pillow==12.3.0               # AutoProcessor image support (llm leaf)
python-dotenv==1.2.2
numpy==2.5.1
