# Client-side environment for train.py / inference.py (ScalarLM job submission).
#
# These scripts are thin CLIENTS: they format data locally and submit train/generate
# jobs to a remote ScalarLM server (SCALARLM_API_URL). Heavy training deps (torch,
# transformers, megatron, flash-attn, ...) live on the SERVER, not here. The client is
# pure Python and needs no GPU.
#
# scalarlm is not pinned here; `pip install scalarlm` installs the `masint` package plus
# the `scalarlm` CLI. See https://github.com/supermassive-intelligence/scalarlm
scalarlm

# Client-side helpers used directly by train.py / inference.py
python-dotenv==1.2.2
PyYAML==6.0.3

# NOTE: the ml/ subtree (cray_megatron, tokenformer) runs SERVER-SIDE; its dependencies
# are managed by the ScalarLM deployment, not by this file.
