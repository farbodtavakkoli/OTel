# Pinned environment for train_llm_deepspeed_standalone.py (Python 3.12).
# Standalone HF Transformers + DeepSpeed (ZeRO) SFT trainer (full fine-tuning, SFT only).
# Core training stack
torch==2.11.0          # CUDA 13 build on PyPI; the ROCm build pins this exactly — see notes below
deepspeed==0.19.4

# NVIDIA / CUDA 13 — torch==2.11.0 on PyPI is a CUDA 13 build, so this file installs as-is
# with no --index-url. Re-verify torch afterwards:
#   pip install -r requirements_standalone.txt
#   python -c "import torch;print(torch.__version__, torch.version.cuda)"   # 2.11.0 13.0
# To pin the +cu130 local version tag explicitly, install torch first with
# --index-url https://download.pytorch.org/whl/cu130.
# deepspeed==0.19.4 installs from PyPI unchanged (JIT ops build on demand). The default
# adamw_bnb_8bit optimizer works on the CUDA bitsandbytes wheel (adamw_torch is the fallback).

# AMD / ROCm 7.2 — install torch from the ROCm wheel index first, then this file (the
# torch==2.11.0 pin is satisfied by the +rocm7.2 build and left alone):
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/rocm7.2
#   pip install -r requirements_standalone.txt
# deepspeed==0.19.4 installs from PyPI unchanged as a pure-python wheel (no hipcc at
# install time; ops JIT on demand). bitsandbytes==0.50.0 PyPI wheel ships ROCm binaries
# and works on gfx950 with the default adamw_bnb_8bit optimizer.
# Export CUDA_HOME=/opt/rocm on a ROCm box — the script otherwise defaults it to
# /usr/local/cuda-13 and prepends phantom CUDA paths.
# Do not pip-install the CUDA flash-attn wheel on ROCm; the main path does not use it.

# HF training stack
transformers==5.5.0
trl==0.24.0
datasets==4.3.0
accelerate==1.14.0
bitsandbytes==0.50.0
tokenizers==0.22.2
huggingface_hub==1.27.0
safetensors==0.8.0
sentencepiece==0.2.2

# Logging / kernels DeepSpeed builds against
tensorboard==2.21.0
ninja==1.13.0

# Utilities
python-dotenv==1.2.2
numpy==2.5.1
protobuf==7.35.1
tqdm==4.70.0
PyYAML==6.0.3
