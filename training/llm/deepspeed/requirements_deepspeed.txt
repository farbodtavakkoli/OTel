# Pinned environment for train_llm_deepspeed.py (Python 3.12).
# Multi-GPU HF Transformers + DeepSpeed (ZeRO) trainer: SFT / DPO / GRPO.
#
# NVIDIA / CUDA 13 — install torch and torchvision FIRST; the pins below are the cu130
# builds, so no --index-url is needed:
#   pip install torch==2.11.0 torchvision==0.26.0 numpy
#   pip install -r <this file minus the torch/torchvision lines>
#   python -c "import torch;print(torch.__version__,torch.version.cuda)"  # re-check: NOT clobbered
#
# Do NOT pip-install flash-attn: the script hard-blocks flash_attention_2 for gemma-4
# (head_dim > 256) — use --flash_attention sdpa.
#
# Core training stack
torch==2.11.0
torchvision==0.26.0
deepspeed==0.19.4

# AMD / ROCm 7.2 — same order, but torch comes from the ROCm wheel index, which satisfies
# the pins exactly (torch 2.11.0+rocm7.2, torchvision 0.26.0+rocm7.2):
#   pip install torch==2.11.0 torchvision --index-url https://download.pytorch.org/whl/rocm7.2
#   pip install -r <this file minus the torch/torchvision lines>
# deepspeed==0.19.4 installs from PyPI unchanged on both platforms (pure-python wheel).
# bitsandbytes PyPI wheels include ROCm builds (Instinct gfx90a/gfx942/gfx950).
# Do NOT pip-install flash-attn on ROCm either — the CUDA wheel cannot build.
#
# DPO/GRPO extras (platform-independent, also needed on CUDA with these pins):
# trl 0.24.0's optional-dependency probes misfire under transformers 5.5.0, so importing
# DPOTrainer/GRPOTrainer requires:
#   pip install mergekit            # then re-run: pip install accelerate==1.14.0
#                                   # (mergekit downgrades accelerate; force the pin back)
# plus tiny import stubs for llm_blender and weave in site-packages (the real
# packages are incompatible with transformers 5.5.0) — see readme_deepspeed.md.

# HF training stack
transformers==5.5.0
trl==0.24.0
peft==0.20.0
datasets==4.3.0
accelerate==1.14.0
bitsandbytes==0.50.0
tokenizers==0.22.2
huggingface_hub==1.27.0
safetensors==0.8.0
sentencepiece==0.2.2
hf_transfer==0.1.9
hf-xet==1.6.0

# Logging / kernels DeepSpeed builds against
tensorboard==2.21.0
ninja==1.13.0
einops==0.8.2

# Utilities used directly by the script / data loading
python-dotenv==1.2.2
numpy==2.5.1
protobuf==7.35.1
tqdm==4.70.0
PyYAML==6.0.3
filelock==3.32.2
packaging==26.3
