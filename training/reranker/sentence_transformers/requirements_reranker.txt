# Environment for train_reranker_standalone.py (sentence-transformers CrossEncoder reranker).
# Install into a dedicated venv.
#
# AMD/ROCm 7.2 — install the ROCm torch wheel FIRST, then the rest of this file:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/rocm7.2
#   pip install -r requirements_reranker.txt
# Skip flash-attn (CUDA-only build); the script falls back to "sdpa" automatically.
#
# NVIDIA/CUDA 13 — no --index-url needed; the ==2.11.0 pin has a native cu130 wheel.
# Install torch first, then the rest of this file:
#   pip install torch==2.11.0 numpy
#   pip install -r requirements_reranker.txt
#   python -c "import torch;print(torch.__version__)"   # re-verify: not clobbered
# flash-attn has no prebuilt wheel for torch 2.11/cu130/py3.12; build from source if you
# want flash_attention_2, otherwise the script uses "sdpa" with no edit.
#
# Core reranker stack
sentence-transformers==5.7.0

# Training stack
torch==2.11.0   # Same pin on both vendors: +rocm7.2 from the ROCm index, +cu130 from PyPI.
accelerate==1.14.0
transformers==5.5.0
datasets==4.3.0

# Optional (CUDA only): flash-attn, used when it imports.
# flash-attn==2.8.3

# Utilities
python-dotenv==1.2.2
tensorboard==2.21.0
numpy==2.5.1
