# Environment for train_embedding_standalone.py (sentence-transformers embedding fine-tuning).
# Install into a dedicated venv.
#
# AMD/ROCm 7.2 — install the ROCm torch wheel FIRST, then the rest of this file:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/rocm7.2
#   pip install -r requirements_embedding.txt
# Do NOT install flash-attn on ROCm (CUDA-only build) — load_model() auto-selects "sdpa".
#
# NVIDIA/CUDA 13 — no --index-url needed; the ==2.11.0 pin has a native cu130 wheel:
#   pip install torch==2.11.0 numpy==2.5.1
#   pip install -r requirements_embedding.txt
# Re-check torch.version.cuda == '13.0' afterwards — some deps clobber torch.
#
# Core embedding stack
sentence-transformers==5.7.0
mteb==2.18.16                 # code targets mteb>=2.7.8 (uses mteb.evaluate / ResultCache)
model2vec==0.8.2             # only needed for the static "potion-*" (model2vec) models

# Training stack
torch==2.11.0
accelerate==1.14.0
transformers==5.5.0
datasets==4.3.0

# Optional (CUDA only): flash-attn for attn_implementation="flash_attention_2".
# No prebuilt wheel exists for torch 2.11+cu130, so build from source:
#   CUDA_HOME=/usr/local/cuda-13.0 pip install flash-attn==2.8.3 --no-build-isolation
# flash-attn==2.8.3

# Utilities
python-dotenv==1.2.2
tensorboard==2.21.0
numpy==2.5.1
