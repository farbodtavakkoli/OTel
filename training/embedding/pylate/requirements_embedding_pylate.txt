# Environment for train_embedding_pylate.py (PyLate ColBERT late-interaction fine-tuning).
# Install into a DEDICATED venv: pylate hard-pins sentence-transformers==5.3.0 /
# transformers<=5.3.0 and WILL downgrade an existing 5.7.0 / 5.5.0 install.
#   python3 -m venv $DATA_DIR/envs/.env_pylate
#   source $DATA_DIR/envs/.env_pylate/bin/activate
#   export PIP_CACHE_DIR=$DATA_DIR/pip_cache
#
# AMD/ROCm — install the ROCm torch wheel FIRST, then this file:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/rocm7.2
#   pip install -r requirements_embedding_pylate.txt
#   python -c "import torch; print(torch.__version__, torch.version.hip)"  # 2.11.0+rocm7.2 7.2.26015
# If a CUDA torch ever lands on top, recover with:
#   pip install --force-reinstall --no-deps torch==2.11.0 \
#     --index-url https://download.pytorch.org/whl/rocm7.2
# Do NOT install flash-attn on ROCm (CUDA-only build) — the script auto-selects "sdpa".
#
# NVIDIA (CUDA 13) — drop the --index-url line; torch==2.11.0 resolves to +cu130 on PyPI:
#   pip install "torch==2.11.0" numpy
#   pip install -r requirements_embedding_pylate.txt
# Optional CUDA-only extras (not required): flash-attn, pylate[flash-maxsim], pylate[lik].

# Core late-interaction stack
pylate==1.6.0

# Hard-pinned by pylate 1.6.0; listed explicitly so the versions are visible.
sentence-transformers==5.3.0
transformers==5.3.0
tokenizers==0.22.2

# Training stack
torch==2.11.0
accelerate==1.14.0
datasets==5.0.1
safetensors==0.8.0

# PyLate index/retrieval backends (pulled in by pylate; pinned for reproducibility)
fast-plaid==1.4.6.2110        # Rust PLAID index — prebuilt wheel, no CUDA toolchain needed
fastkmeans==0.5.0
usearch==2.26.0
ninja==1.11.1.4
ujson==5.12.0

# Utilities
python-dotenv==1.2.2
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.0
